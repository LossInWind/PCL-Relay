[project]
name = "pcl-codex-bridge"
version = "0.1.0"
description = "Tailnet-only PCL model gateway and Codex execution agents"
requires-python = ">=3.9"
license = {text = "MIT"}

[project.scripts]
pcl-codex = "pcl_codex_bridge.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["execution_fixture"]
