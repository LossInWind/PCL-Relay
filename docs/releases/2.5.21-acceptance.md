# 2.5.21 acceptance — 2026-09-25

Code: `d66e863`; OpenCodex remains 2.63.0, pinned vendor tree
`22639518db6eab88525f7f0932cc4a81a7573b6d` unchanged.

## Verification

- 252 Python tests; Swift release build; standalone Swift export assertions pass.
  XCTest is unavailable on the build host, not reported as passed.
- Isolated OpenCode 2.0.16 replay of a copied history: GLM-5.2 and
  DeepSeek-V4-Pro contain zero image parts, retain 12 tools and High.
- Original OpenCode config backed up; semantic comparison proves only the two
  capability objects changed. No session or credential file was modified.
- GLM and DeepSeek each complete ordinary and streaming tool requests with High
  through an isolated gateway, then again through the deployed Tailnet gateway.
  Diagnostic tools were not executed. This does not certify quality differences
  between effort settings or the original complex presentation task.
- Deployed loopback GLM image request and Tailnet DeepSeek image request return
  status 400, `unsupported_image_input`, Chinese explanation and request ID.
- Installed Mac GUI inspected with native accessibility and screenshot; model
  notices visible, API export button returns copied confirmation.
- Mac OpenCodex PID 2003 and 3070Ti PID 1747 unchanged. User explicitly authorized
  gateway restart; only two 3070Ti Relay gateways and control-sync services were
  restarted, not OpenCodex, SSH, VPN or mounts.

## Release provenance

GitHub Actions run `36104487059` builds both Linux architectures from `d66e863`.
Its draft-upload step raced with the local Linux upload. Publication was paused;
CI Linux assets were downloaded and checksum-verified and release metadata was
regenerated to refer to them. The Mac bundle uses rebuilt GUI and unchanged pinned
embedded runtimes. Published release: `v2.5.21`.

- macOS SHA256: `e401c53e62c65ce24ed9fd643d0a4acc75b78ec716310e031232e30a5037056e`
- Linux x86_64 SHA256: `9ad042de7e45978ff95d472ab31a1ebde8e8326f18d5e770c3d0bf096799cecb`
- Linux aarch64 SHA256: `75206834d550b5b2e4aef86ee61733433b52e8d59ce55d3e5a78f1099810826f`

## Device receipt

| Device | Installed | Runtime / source |
| --- | --- | --- |
| Local Mac | 2.5.21 | GUI d66e863 and control sync 2.5.21; OpenCodex untouched |
| 3070Ti | 2.5.21 | Both gateways and control sync activated; real requests pass; GitHub transfer timed out then identical verified Mac cache used |
| bupt-a100-shared | 2.5.21 | GitHub asset installed; CLI verified; no active control-sync service, no model process restart |
| 10.112.205.245 | 2.5.21 | GitHub TLS timeout then identical verified Mac cache; model processes not restarted |
| A6000 Pod | Pending | SSH timeout; not upgraded |
| Kai Mac | Pending | SSH timeout; not upgraded |

All installation receipts verify Codex config/auth hashes unchanged. Runtime
activation is not inferred from disk installation on other hosts. Kimi's original
work remains untouched; its task-level completion is not claimed by this patch.
